---
phase: quick-260704-u3n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/005_sales_ghl.sql
  - api/server.js
  - index.html
autonomous: true
requirements: [QUICK-260704-U3N]

must_haves:
  truths:
    - "Un miembro activo (no solo admin) puede pedir GET /api/ghl/leads y recibe leads con cita de SU org (OAuth) o de la instancia (PIT), o un 501 claro si falta configuración"
    - "Al registrar una venta con lead GHL asociado, el tracker guarda cuotas/primer_pago/fue_reserva/ghl_contact_id y dispara best-effort custom fields + tag + oportunidad + Slack en GHL"
    - "Sin GHL configurado, la vista Ventas sigue funcionando con carga manual: el formulario cae solo al input de nombre y el botón dice 'Registrar venta'"
    - "En Cargar día, doble clic sobre un contador abre un input numérico para escribir el valor directo (Enter confirma, Escape cancela)"
    - "El rail muestra Ventas ARRIBA de Cargar día, Metas sigue visible, y los ítems del rail son operables por teclado (Tab + Enter/Espacio)"
    - "El código del repo no contiene ningún ID de la subcuenta de Clara ni secretos (todo por env)"
  artifacts:
    - path: "supabase/migrations/005_sales_ghl.sql"
      provides: "Columnas primer_pago, fue_reserva, ghl_contact_id en st_sales (idempotente)"
      contains: "add column if not exists primer_pago"
    - path: "api/server.js"
      provides: "requireMember, getGhlCreds, GET /api/ghl/leads, POST /api/sales/ghl"
      exports: ["/api/ghl/leads", "/api/sales/ghl"]
    - path: "index.html"
      provides: "editNum/setNum, rail reordenado, a11y/móvil, vista Ventas extendida con loadGhlLeads + fallback manual"
      contains: "loadGhlLeads"
  key_links:
    - from: "index.html"
      to: "/api/ghl/leads"
      via: "fetch en loadGhlLeads con Bearer del session token"
      pattern: "fetch\\('/api/ghl/leads'"
    - from: "index.html"
      to: "/api/sales/ghl"
      via: "fetch best-effort post-insert en addSale"
      pattern: "fetch\\('/api/sales/ghl'"
    - from: "api/server.js"
      to: "st_integrations vía getIntegration + refreshGhlToken"
      via: "getGhlCreds(orgId)"
      pattern: "getGhlCreds"
    - from: "api/server.js"
      to: "app.mazefunnels.com deep-link"
      via: "mensaje Slack (regla white-label)"
      pattern: "app\\.mazefunnels\\.com/v2/location"
---

<objective>
Portar al producto (repo `maze-sales-tracker`, rama `develop`) las mejoras FUNCIONALES parcheadas a mano en la instancia de Clara: módulo Ventas conectado a GHL (leads con cita + cierre de ciclo custom fields/tag/oportunidad/Slack), doble clic en contadores de Cargar día, reorden del rail y accesibilidad/móvil — **adaptando todo a multi-tenant** (credenciales GHL por org vía st_integrations/OAuth, con fallback a PIT por env para instancias dedicadas) y **sin portar nada del branding dorado de Camino Digital**.

Purpose: que la próxima actualización de la instancia de Clara salga del repo (no de parches locales) y que cualquier tenant con OAuth conectado tenga el mismo módulo Ventas-GHL.
Output: migración 005 idempotente + endpoints `/api/ghl/leads` y `/api/sales/ghl` en la mini-API + index.html con capture UX, rail, a11y y vista Ventas extendida.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260704-u3n-portar-mejoras-de-la-instancia-de-clara-/clara-instance-reference.diff  ← EL DIFF DE REFERENCIA: fuente de qué portar. Leerlo COMPLETO antes de tocar código.
@api/server.js
@index.html
@supabase/migrations/001_sales_tracker.sql

**Reglas de oro de este port:**
1. El diff de referencia fue escrito para mono-tenant con PIT estático e IDs hardcodeados de la subcuenta de Clara. NO copiarlo ciego: adaptar a multi-tenant según las instrucciones de cada task.
2. Repo público: CERO secretos y CERO IDs de la subcuenta de Clara en el código. Los IDs de custom fields y pipeline entran por env (`GHL_CF_JSON`, `GHL_PIPELINE_JSON`).
3. NO portar (branding de Clara): título "Camino Digital", cambios de color (`#DDB65F`, `rgba(221,182,95,…)`), bloque `<style id="clara-brand">`, fonts Cormorant/Montserrat, footer "Camino Digital", brand del login, texto "Sin metas cargadas por ahora", ocultamiento de Metas, docker-compose.yml. El producto queda lima/Maze intacto.
4. No hay SSH ni deploy en esta ejecución. La verificación es local (node --check, arranque con env dummy, grep gates).

<interfaces>
<!-- Contratos existentes en api/server.js que los endpoints nuevos DEBEN reutilizar -->

De api/server.js (ya existen, no recrear):
```javascript
function sendJSON(res, status, obj)                 // respuesta JSON estándar
function readJSONBody(req)                          // -> {ok, data}
function svcHeaders(extra = {})                     // headers service-role Supabase
async function checkUserToken(bearerToken)          // valida JWT contra GoTrue -> {ok, uid} | {ok:false, status, error}
async function getIntegration(orgId)                // fila completa de st_integrations o null (tokens incluidos, solo server-side)
async function refreshGhlToken(integration)        // -> access_token vigente (refresca+persiste si vence en <5min; lanza Error si falla)
```

De index.html (ya existen, no recrear):
```javascript
sb                    // cliente supabase-js; sesión: const {data:{session}} = await sb.auth.getSession()
DB                    // {team:{id,...}, members:[{id,name,role}], sales:[...], entries:{...}, goals:{...}}
ME, IS_ADMIN          // perfil propio y flag admin
esc(s), toast(msg,ms), todayStr(), money(n), num(n)
membersByRole(role)   // miembros activos por rol
renderSales()         // línea ~1330; addSale en ~1364; footer() en ~1381
renderCapture()       // línea ~834; window.bump en ~884; window.setMoney en ~894
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migración 005 + endpoints GHL de ventas en la mini-API (multi-tenant)</name>
  <files>supabase/migrations/005_sales_ghl.sql, api/server.js</files>
  <action>
**1a. Crear `supabase/migrations/005_sales_ghl.sql`** — idempotente (en la base viva del VPS estas columnas YA existen, agregadas a mano para Clara: la migración debe ser no-op ahí). Contenido: comentario de encabezado explicando el contexto + `alter table public.st_sales add column if not exists primer_pago numeric not null default 0;`, `alter table public.st_sales add column if not exists fue_reserva boolean not null default false;`, `alter table public.st_sales add column if not exists ghl_contact_id text;` (tres statements `alter table ... add column if not exists`, estilo de 001).

**1b. En `api/server.js`, agregar lectura de env nuevas** junto al bloque GHL existente (línea ~28): `GHL_PIT`, `GHL_LOCATION`, `GHL_CALENDAR`, `SLACK_TOKEN`, `SLACK_WINS_CHANNEL` (strings, default ''), y dos JSON parseados con tolerancia al arranque: `GHL_CF` desde `process.env.GHL_CF_JSON` (claves esperadas: montoTotal, cantidadPagos, metodoPago, programa, montoReserva, fueReserva, primerPago, vendedor) y `GHL_PIPELINE` desde `process.env.GHL_PIPELINE_JSON` (claves: pipelineId, stageReserva, stagePago). Si el JSON falta o es inválido → la constante queda `null` y se loguea un aviso (`[api] GHL_CF_JSON no configurado/ inválido — se saltea el paso de custom fields`), NUNCA crashear el arranque. Definir `const GHL_BASE = 'https://services.leadconnectorhq.com'` y un helper `ghlHeaders(token, version = '2021-07-28')` que recibe el token como parámetro (a diferencia del diff, que usaba el PIT global — acá el token varía por org).

**1c. Helper `requireMember(req)`** — cualquier miembro ACTIVO del equipo (no solo admin). Integrarlo con el estilo actual: reusar `checkUserToken(req.headers['authorization'] || '')` para validar el JWT, y después leer el perfil con `svcHeaders()` pidiendo `select=org_id,role,name,active`. Si el perfil no existe o `active === false` → `{ok:false, status:403, error:'Tu usuario no está activo en el equipo'}`. Si ok → `{ok:true, uid, org_id, role, name}`. (El diff trae su propia versión standalone en las líneas 42-71; el comportamiento es ese, la implementación reusa los helpers del repo.)

**1d. Helper `getGhlCreds(orgId)`** — resolución de credenciales GHL por org: (1) `const integration = await getIntegration(orgId)`; si hay fila → `{token: await refreshGhlToken(integration), locationId: integration.location_id}` (dejar que el Error de refresh burbujee al caller, que responde 502). (2) Si NO hay integración pero existen `GHL_PIT` y `GHL_LOCATION` (modo instancia dedicada, ej. Clara) → `{token: GHL_PIT, locationId: GHL_LOCATION}`. (3) Si nada → `null`.

**1e. `GET /api/ghl/leads`** — portar `ghlLeads` del diff (líneas 73-118) con estas adaptaciones: (a) recibe `member` de `requireMember`; (b) creds por org vía `getGhlCreds(member.org_id)` — si `null` → 501 `{error:'GHL no está configurado en esta instancia'}`; (c) `calendarId` desde env `GHL_CALENDAR` — si falta → 501 `{error:'Falta configurar el calendario de admisión (GHL_CALENDAR)'}`; (d) **cache POR ORG**: `const leadsCache = new Map()` (orgId → {at, data}), TTL 60s — no la variable global única del diff; (e) todas las llamadas GHL usan `ghlHeaders(creds.token, ...)` y `creds.locationId`. Mantener del diff: ventana −14/+7 días, filtro de citas cancelled/invalid, dedupe por contactId quedándose con la cita más reciente, fetch del contacto por cid con dedupe de palabras del nombre, sort desc por cita, respuesta `{leads}` / `{leads, cached:true}`.

**1f. `POST /api/sales/ghl`** — portar `salesGhl` del diff (líneas 120-199) con estas adaptaciones: (a) creds por org vía `getGhlCreds(member.org_id)` — si `null` → 501; (b) **custom fields**: usar el objeto `GHL_CF` parseado del env — si `GHL_CF` es `null`, SALTEAR el paso completo (result.customFields queda false) sin fallar; mismo mapeo del diff (montoTotal←facturado, cantidadPagos←cuotas, metodoPago←metodo, programa, primerPago, vendedor←b.vendedor||member.name, fueReserva 'Si'/'No', montoReserva solo si fueReserva); (c) **tag**: igual al diff (`reserva` / `venta-cerrada`); (d) **oportunidad**: pipelineId/stages desde `GHL_PIPELINE` — si es `null` o le falta pipelineId, SALTEAR el paso; usar `creds.locationId`, stage `stageReserva`/`stagePago` según fueReserva; NO hardcodear ningún ID (los del diff son de la subcuenta de Clara — PROHIBIDO copiarlos); (e) **Slack**: igual al diff con env `SLACK_TOKEN`/`SLACK_WINS_CHANNEL` (saltear si faltan); el deep-link del contacto se mantiene con `https://app.mazefunnels.com/v2/location/${creds.locationId}/contacts/detail/${contactId}` (regla white-label — NUNCA app.gohighlevel.com); (f) todo best-effort con try/catch por paso, respuesta `{ok:true, tag, ...result}` y log con member.uid como en el diff.

**1g. Router**: registrar las dos rutas ANTES del bloque `/api/members` (junto a las rutas GHL existentes, ~línea 1060): `GET /api/ghl/leads` y `POST /api/sales/ghl`, ambas con `const member = await requireMember(req); if (!member.ok) return sendJSON(res, member.status, {error: member.error});`. Cuidado: NO colisionar con la ruta existente `GET /api/ghl/users` (admin). Actualizar el comentario de rutas del encabezado del archivo (líneas 7-16) con las dos rutas nuevas.
  </action>
  <verify>
    <automated>node --check api/server.js && grep -c "add column if not exists" supabase/migrations/005_sales_ghl.sql | grep -q "^3$" && ! grep -rn "3ydYygncRnvwpT1bFadV\|QFrdanHs6rF7DrlKn6NF\|36247118-bb6c\|8981ba62-578a\|p4JMHWMKfpI6CQiFcBBN\|Ppg3ITdJxPPrGUcaYeAF" api/ index.html supabase/ && grep -q "app.mazefunnels.com/v2/location" api/server.js && SUPABASE_URL=http://localhost:9999 SERVICE_ROLE_KEY=x ANON_KEY=x PORT=3999 node api/server.js & sleep 1; curl -s http://localhost:3999/api/health | grep -q '"ok":true' && curl -s -o /dev/null -w "%{http_code}" http://localhost:3999/api/ghl/leads | grep -q "401\|502"; RC=$?; kill %1 2>/dev/null; exit $RC</automated>
  </verify>
  <done>Migración 005 existe con las 3 columnas idempotentes. `node --check` pasa. La API arranca sin NINGÚN env GHL/Slack (solo avisos en log). `/api/ghl/leads` sin token responde 401 coherente (el 501 por falta de GHL queda detrás del auth, verificado por lectura del código). Cero IDs de la subcuenta de Clara en el repo.</done>
</task>

<task type="auto">
  <name>Task 2: index.html — doble clic en contadores, rail reordenado y accesibilidad/móvil</name>
  <files>index.html</files>
  <action>
Portar del diff de referencia SOLO lo funcional (nada de colores/fonts/branding):

**2a. Doble clic en contadores de Cargar día** (diff líneas 490-531): en `renderCapture` (~línea 853 actual), agregar al `<span class="num" id="n_${mt.k}">` los atributos `ondblclick="editNum('${mt.k}')"` y `title="Doble clic para escribir el número"`. Después de `window.bump` (~línea 893), agregar `window.setNum=(k,val)=>{...}` y `window.editNum=(k)=>{...}` COPIADOS TAL CUAL del diff (setNum: clamp a >=0 + Math.round, save(), actualizar `n_`+k, refrescar capSum, flashSave; editNum: input numérico inline con los estilos css del diff — usa `var(--lima)` que en el producto es lima, correcto —, focus+select, Enter=commit / Escape=cancel / blur=commit con guarda `done`).

**2b. Rail** (diff líneas 462-469, ADAPTADO): mover el `<div class="nav-i" data-view="sales" ...>` (actualmente línea ~341) para que quede INMEDIATAMENTE ARRIBA del `data-view="capture"` (línea ~337). Orden final: dashboard → **sales** → capture → table → team → goals. **Metas (`data-view="goals"`) QUEDA VISIBLE** — su ocultamiento en el diff fue un pedido puntual para Clara, NO portarlo. No tocar brand ni los nav-i de acciones (theme/password/logout).

**2c. Accesibilidad teclado** (diff línea 477): en el `document.querySelectorAll('.nav-i').forEach(el=>{...})` (~línea 560), agregar como primera línea del callback: `el.tabIndex=0; el.addEventListener('keydown',e=>{ if(e.key==='Enter'||e.key===' '){ e.preventDefault(); el.click(); } });`.

**2d. CSS funcional** (diff líneas 356-364): junto a `.inp:focus` (~línea 170) agregar `.inp:disabled{opacity:.4;cursor:not-allowed}`, la media query móvil `@media(max-width:520px){ .cap-intro p{display:-webkit-box;-webkit-line-clamp:1;-webkit-box-orient:vertical;overflow:hidden} .cap-intro.open p{display:block;-webkit-line-clamp:unset} .nav-i .tt{display:none} }` y `.nav-i:focus-visible{outline:2px solid var(--lima);outline-offset:2px}`. NO tocar ninguna regla de color existente.

**2e. cap-intro colapsable** (diff línea 501): al `<div class="cap-intro">` de renderCapture (~línea 858) agregarle `onclick="this.classList.toggle('open')"`.
  </action>
  <verify>
    <automated>grep -q 'ondblclick="editNum' index.html && grep -q "window.setNum" index.html && grep -q "window.editNum" index.html && grep -q "nav-i:focus-visible" index.html && grep -q ".inp:disabled" index.html && grep -q "cap-intro.open p" index.html && grep -q 'data-view="goals"' index.html && test "$(grep -n 'data-view="sales"' index.html | head -1 | cut -d: -f1)" -lt "$(grep -n 'data-view="capture"' index.html | head -1 | cut -d: -f1)" && node -e "const s=require('fs').readFileSync('index.html','utf8');const m=[...s.matchAll(/<script>([\s\S]*?)<\/script>/g)];m.forEach(x=>new Function(x[1]));console.log('JS inline OK:',m.length,'bloques')"</automated>
  </verify>
  <done>Doble clic en un contador abre input inline (Enter/Escape/blur). Ventas aparece arriba de Cargar día en el rail y Metas sigue visible. Los nav-i son focusables y operables con Enter/Espacio con anillo de foco lima. En móvil el instructivo colapsa a 1 línea (tap expande) y los tooltips del rail no aparecen. `bump`/`setMoney`/steppers existentes intactos (sin cambios en su código). Sintaxis JS inline válida.</done>
</task>

<task type="auto">
  <name>Task 3: index.html — vista Ventas extendida con leads GHL y disparo de onboarding</name>
  <files>index.html</files>
  <action>
Portar la vista Ventas del diff (líneas 535-628) ADAPTADA para degradar sin GHL:

**3a. Formulario extendido en `renderSales`** (~línea 1344): reemplazar el campo Cliente por el par del diff: `<div class="field" id="s_lead_wrap" style="display:none"><label>Cliente (estuvo en llamada)</label><select class="inp" id="s_lead" onchange="...toggle s_cliente_wrap..."><option value="">Cargando leads con cita…</option></select></div>` + `<div class="field" id="s_cliente_wrap"><label>Nombre del cliente</label><input class="inp" id="s_cliente" placeholder="Nombre"></div>`. **Diferencia clave con el diff:** el select arranca OCULTO y el input manual VISIBLE (el diff hacía lo inverso); `loadGhlLeads` los intercambia solo si hay leads (ver 3b). Agregar los campos nuevos del diff: `s_cuotas` (number, min 1, value 1), `s_primerpago` (number), `s_reservaflag` (select No/Sí con el onchange del diff que habilita/deshabilita `s_reserva`), `s_reserva` (number, disabled inicial). Mantener placeholders neutros del producto en Programa ("High Ticket / Membresía / Lite") y montos — NO los de Clara ("Mentoría 4 meses…"). Triage: `${triages.length?`<div class="field"><label>Triage</label><select class="inp" id="s_triage">${opt(triages)}</select></div>`:`<input type="hidden" id="s_triage" value="">`}` como en el diff. Botón: darle `id="s_submit"` con texto por defecto **"Registrar venta"** (loadGhlLeads lo cambia si hay GHL, ver 3b). Al final de `renderSales`, llamar `loadGhlLeads();` como en el diff.

**3b. `loadGhlLeads()` + `GHL_LEADS`** (diff líneas 570-585, ADAPTADO con fallback): declarar `let GHL_LEADS=[];` a nivel módulo. La función: obtener session, `fetch('/api/ghl/leads', {headers:{Authorization:'Bearer '+session.access_token}})`. **Fallback obligatorio:** si el fetch lanza, `!r.ok` (incluye 501), o `!j.leads || !j.leads.length` → dejar `GHL_LEADS=[]`, `s_lead_wrap` oculto, `s_cliente_wrap` visible, botón "Registrar venta", y RETURN sin romper la vista (sin toast de error — la ausencia de GHL es un estado normal). Si hay leads: poblar el select con el formato del diff (`— Elegí el lead de la llamada —` + opciones `${esc(c.id)}` / `${esc(name · fecha)}` + `Otro (escribir a mano, sin GHL)` con value `__manual__`), mostrar `s_lead_wrap`, ocultar `s_cliente_wrap` (el onchange del select lo re-muestra si eligen `__manual__`), y cambiar el texto del botón a **"Registrar venta y disparar onboarding"**.

**3c. `addSale` extendido** (diff líneas 587-628): resolver cliente/ghlId como el diff (si `s_lead` tiene valor ≠ `__manual__` → name/id del lead de `GHL_LEADS`; si no → `s_cliente` manual; validar con toast 'Elegí el lead o escribí el cliente'). El `row` del insert suma: `cuotas:+g('s_cuotas')||1, reserva:+g('s_reserva')||0, primer_pago:+g('s_primerpago')||0, fue_reserva:fueReserva, ghl_contact_id:ghlId` (resto igual al actual). El push a `DB.sales` conserva el shape actual (keys `closer/triage/setter`) sumando cuotas/reserva reales. **Post-insert best-effort** (solo si `ghlId`): POST `/api/sales/ghl` con el body del diff (contactId, clienteNombre, programa, facturado, cash, cuotas, metodo, fueReserva, reserva, primerPago, vendedor = nombre del closer via `DB.members.find`), con los toasts del diff: éxito → `'Reserva registrada — tag "reserva" aplicado 🔒'` / `'Venta registrada — onboarding disparado 🎉'`; fallo/catch → `'Venta guardada, pero GHL falló: revisá el contacto'`. Sin ghlId → toast `'Venta registrada'` (a secas — no mencionar GHL/onboarding cuando no aplica, para no confundir a tenants sin GHL). Cerrar con `renderSales()`.

**NO tocar:** footer(), título de la página, textos de login, delSale, columnas del historial.
  </action>
  <verify>
    <automated>grep -q "s_lead_wrap" index.html && grep -q "s_cliente_wrap" index.html && grep -q "loadGhlLeads" index.html && grep -q "fetch('/api/sales/ghl'" index.html && grep -q "fetch('/api/ghl/leads'" index.html && grep -q "primer_pago:" index.html && grep -q "ghl_contact_id:" index.html && grep -q "__manual__" index.html && ! grep -q "Camino Digital" index.html && ! grep -q "DDB65F" index.html && ! grep -q "clara-brand" index.html && grep -q "Maze Sales Tracker IA · v1 funcional" index.html && node -e "const s=require('fs').readFileSync('index.html','utf8');const m=[...s.matchAll(/<script>([\s\S]*?)<\/script>/g)];m.forEach(x=>new Function(x[1]));console.log('JS inline OK')"</automated>
  </verify>
  <done>La vista Ventas muestra el formulario extendido; sin GHL (endpoint 501/error/0 leads) cae sola al input manual con botón "Registrar venta" y el insert funciona igual que hoy (+campos nuevos con defaults). Con leads cargados aparece el selector y el botón "Registrar venta y disparar onboarding"; la venta con lead dispara el POST best-effort con los toasts del diff. Cero rastro de branding Camino Digital en el repo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → mini-API | JWT del cliente cruza; body de /api/sales/ghl es input no confiable |
| mini-API → GHL/Slack | tokens sensibles salen; respuestas externas no confiables |
| repo público → mundo | cualquier string commiteado es público |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-u3n-01 | Information Disclosure | repo público | mitigate | IDs de custom fields/pipeline y tokens SOLO por env (GHL_CF_JSON, GHL_PIPELINE_JSON, GHL_PIT, SLACK_TOKEN); grep gate en Task 1 bloquea los IDs de Clara |
| T-u3n-02 | Elevation of Privilege / cross-tenant | GET /api/ghl/leads, POST /api/sales/ghl | mitigate | requireMember valida JWT contra GoTrue + perfil activo; creds SIEMPRE resueltas por member.org_id server-side (getGhlCreds), jamás del body |
| T-u3n-03 | Information Disclosure | cache de leads | mitigate | cache Map POR org (orgId como key) — una cache global filtraría leads entre tenants |
| T-u3n-04 | Tampering (XSS) | select de leads en index.html | mitigate | esc() en id y label de cada option (nombres/emails vienen de GHL, no confiables) |
| T-u3n-05 | Spoofing | POST /api/sales/ghl body | accept | contactId arbitrario solo escribe en la subcuenta GHL de la PROPIA org del caller (creds por org); bajo riesgo, sin cruce de tenant |
| T-u3n-SC | Tampering | npm/pip/cargo installs | accept | este plan no instala ningún paquete (API sin dependencias npm, front vanilla) |
</threat_model>

<verification>
1. `node --check api/server.js` pasa.
2. API arranca con env dummy de Supabase y SIN ningún env GHL/Slack: solo logs de aviso, `/api/health` 200, `/api/ghl/leads` sin token → 401.
3. Sintaxis de todos los `<script>` inline de index.html válida (new Function).
4. Grep gates: cero IDs de Clara, cero "Camino Digital"/"DDB65F"/"clara-brand", footer Maze intacto, `data-view="sales"` antes que `data-view="capture"`, `data-view="goals"` presente.
5. Lectura de código: el flujo sin GHL en addSale es idéntico al actual salvo columnas nuevas con default; bump/setMoney sin cambios.
</verification>

<success_criteria>
- Migración 005 idempotente commiteada (no-op esperado contra la base viva del VPS).
- `/api/ghl/leads` y `/api/sales/ghl` operativos multi-tenant: OAuth por org primero, fallback PIT por env, 501 claros cuando falta config (creds o GHL_CALENDAR).
- index.html: doble clic en contadores, rail dashboard→sales→capture→…→goals (Metas visible), a11y teclado + móvil, vista Ventas extendida que degrada limpiamente sin GHL.
- Repo público limpio: cero secretos, cero IDs de la subcuenta de Clara, branding lima/Maze intacto.
</success_criteria>

<post_merge>
## Post-merge (orquestador — NO el ejecutor)

1. **Migración 005 en el Supabase del VPS**: correr `005_sales_ghl.sql` — resultado esperado: **no-op** (las 3 columnas ya existen en la base viva, agregadas a mano para Clara). Confirmar que no falla.
2. **Deploy a `sales-tracker-test`** (entorno de prueba, no la instancia de Clara).
3. **QA manual**:
   - Venta manual SIN GHL sigue OK: formulario cae al input de nombre, botón "Registrar venta", insert funciona, historial la muestra.
   - Org con OAuth conectado pero sin `GHL_CALENDAR` en el env → `/api/ghl/leads` responde 501 con "Falta configurar el calendario de admisión (GHL_CALENDAR)" y la vista Ventas degrada al modo manual sin errores visibles.
   - Doble clic en contadores + navegación por teclado + rail reordenado con Metas visible.
4. **Documentar env vars nuevas para la instancia de Clara** (cuando se actualice desde el repo, su docker-compose/env debe sumar): `GHL_CALENDAR` (ya lo tiene), `GHL_CF_JSON` (JSON con los 8 IDs de custom fields que hoy están hardcodeados en su parche local) y `GHL_PIPELINE_JSON` (`{"pipelineId":"…","stageReserva":"…","stagePago":"…"}` del pipeline 03. Onboarding). Los valores viven en `Clientes/Clara Perez/Credenciales/` del vault, NUNCA en el repo.
</post_merge>

<output>
Create `.planning/quick/260704-u3n-portar-mejoras-de-la-instancia-de-clara-/260704-u3n-SUMMARY.md` when done.
</output>
