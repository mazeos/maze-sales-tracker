---
phase: quick-260704-tu4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [api/server.js, index.html]
autonomous: true
requirements: [GHL-RECON-TOTAL]
must_haves:
  truths:
    - "Con GHL conectado, al cargar la lista de usuarios GHL, todo perfil activo sin ghl_user_id cuyo email coincide exacto con un usuario GHL queda auto-vinculado (ghl_user_id seteado) y aparece como 'importado'"
    - "Todo perfil activo no-admin sin ghl_user_id que NO matchea por email queda dado de baja automáticamente (active=false + ban) en esa misma carga"
    - "Los perfiles con role='admin' NUNCA se dan de baja automáticamente aunque no existan en GHL"
    - "La UI muestra un aviso con los nombres auto-vinculados y refresca 'Miembros del equipo' al instante cuando hubo bajas o auto-vinculaciones"
  artifacts:
    - path: "api/server.js"
      provides: "listGhlUsers con auto-vinculación + baja de huérfanos no-admin + auto_linked en el JSON"
      contains: "auto_linked"
    - path: "index.html"
      provides: "Aviso de auto_linked + refresh del equipo local tras removed/auto_linked"
      contains: "GHL_AUTO_LINKED"
  key_links:
    - from: "api/server.js listGhlUsers"
      to: "respuesta JSON"
      via: "sendJSON con auto_linked"
      pattern: "auto_linked"
    - from: "index.html loadGhlUsers"
      to: "loadFromSupabase"
      via: "refresh condicional si removed o auto_linked"
      pattern: "GHL_AUTO_LINKED.*loadFromSupabase|loadFromSupabase.*GHL_AUTO_LINKED"
---

<objective>
Fase 2.2 carcasa GHL — reconciliación total. Decisión locked de Alejandro: con GHL conectado, TODO miembro no-admin del tracker queda 100% vinculado a un usuario GHL. Caso motivador: "Nico" (closer manual del seed, activo, sin ghl_user_id, email nico@camino.digital que NO existe en GHL) sigue apareciendo en "Miembros del equipo" después de conectar GHL.

Purpose: que la lista GHL sea la única fuente de verdad del equipo cuando la integración está conectada — sin miembros manuales fantasma.
Output: `listGhlUsers` ampliado (auto-vincular por email → baja de huérfanos no-admin → baja de desvinculados existente) + campo `auto_linked` en la respuesta + UI con aviso y refresh inmediato del equipo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@api/server.js  (listGhlUsers líneas ~604-691; helpers getAuthEmail ~552, findAuthUserByEmail ~573, svcHeaders ~78; patrón de baja: PATCH active=false + PUT ban_duration '87600h' líneas ~663-686)
@index.html  (estado GHL líneas ~1042; ghlTeamCard/removedBox ~1045-1090; loadGhlUsers ~1106-1132; reset en disconnect ~1035)

<interfaces>
Contrato actual de GET /api/ghl/users (listGhlUsers, api/server.js):

```js
// Respuesta hoy:
return sendJSON(res, 200, { access_code: integration.location_id, users, removed });
// users: [{ ghl_user_id, name, email, ghl_role, status }] con status ∈ nuevo|vinculable|importado|inactivo
// Estructuras ya computadas ANTES del map de users:
//   emailCache = new Map()                      // uid -> email|null (getAuthEmail)
//   emailToProfile = new Map()                  // emailNorm -> perfil SIN ghl_user_id
//   byGhlId = new Map()                         // ghl_user_id -> perfil
// Patrón de baja existente (reutilizar tal cual):
//   PATCH /rest/v1/st_profiles?id=eq.{id}  body {active:false}  Prefer: return=minimal
//   PUT   /auth/v1/admin/users/{id}        body {ban_duration:'87600h'}  (best-effort)
```

Estado del cliente (index.html):

```js
let GHL_USERS=null, GHL_USERS_LOADING=false, GHL_REMOVED=[];   // línea ~1042
// loadGhlUsers (~1106): GHL_REMOVED=data.removed||[]; if(GHL_REMOVED.length) await loadFromSupabase();
// disconnect (~1035): resetea GHL_STATUS, GHL_USERS, GHL_REMOVED
// removedBox en ghlTeamCard (~1056-1060): patrón de aviso a replicar para auto_linked
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ampliar reconciliación en listGhlUsers (auto-vincular + baja de huérfanos no-admin)</name>
  <files>api/server.js</files>
  <action>
En `listGhlUsers` (GET /api/ghl/users), agregar dos pasos de reconciliación ANTES del map que computa `users` (crítico: hoy el status se calcula con `byGhlId`/`emailToProfile` construidos previamente; si el auto-link corre después, los recién vinculados saldrían "vinculable" en vez de "importado"). Orden final del handler:

**Paso 1 — Auto-vincular (nuevo).** Después de construir `emailToProfile` y `byGhlId`, iterar `ghlUsers`: si el usuario GHL tiene email (normalizado lowercase/trim) que existe en `emailToProfile`, y el perfil matcheado tiene `active !== false` y no tiene `ghl_user_id`, entonces:
- PATCH `/rest/v1/st_profiles?id=eq.{p.id}` con body `{ ghl_user_id: u.id }` (Prefer: return=minimal, svcHeaders). NO tocar role, name, active ni contraseña — solo `ghl_user_id`.
- Si el PATCH falla (status fuera de 2xx o excepción): console.error con contexto y `continue` (el perfil queda como "vinculable", sin efectos).
- Si el PATCH funciona: actualizar el estado local para que el map de status salga bien — `p.ghl_user_id = u.id`, `byGhlId.set(u.id, p)`, `emailToProfile.delete(email)` — y acumular `auto_linked.push(p.name)` + console.log de auditoría (org, id, name, ghl_user_id).
- Guardas: si ya existe `byGhlId.get(u.id)` (ese ghl_user_id ya está tomado por otro perfil), saltar sin vincular.

**Paso 2 — Baja de huérfanos manuales (nuevo).** Después del paso 1, iterar `profs`: todo perfil con `active !== false`, `role !== 'admin'` y sin `ghl_user_id` (tras el auto-link) → dar de baja con el MISMO patrón existente de las líneas ~663-686 (PATCH `{active:false}` con manejo de error + continue, luego PUT ban `{ban_duration:'87600h'}` best-effort), acumular `removed.push(p.name)` y console.log de auditoría indicando "huérfano manual con GHL conectado". EXCEPCIÓN INAMOVIBLE: los perfiles con `role === 'admin'` NUNCA se dan de baja acá — el admin de la org puede no ser usuario de la subcuenta GHL; sin esta excepción el dueño se bloquearía a sí mismo al conectar. Nota: como los perfiles dados de baja actualizan `p.active = false` solo en DB, mutar también el objeto local (`p.active = false`) para consistencia con el map de status si el orden lo requiere — o simplemente correr este paso DESPUÉS del map de `users` (los huérfanos manuales no aparecen en la lista GHL de todos modos, así que no afectan el map). Elegir la opción que deje el código más simple; lo obligatorio es que el paso 1 corra ANTES del map de `users`.

**Paso 3 — Baja de desvinculados (existente, sin cambios de lógica).** El loop actual de perfiles con `ghl_user_id` que ya no está en `ghlIds` queda igual.

**Respuesta:** agregar `auto_linked` al JSON final: `sendJSON(res, 200, { access_code: integration.location_id, users, removed, auto_linked })`. Actualizar el console.log resumen para incluir `auto_linked=${auto_linked.length}`.

Actualizar el comentario de cabecera de la ruta (~599-603) para reflejar la reconciliación total: con GHL conectado, GHL es la única fuente de verdad del equipo no-admin (auto-link por email o baja automática).

Repo público: cero secretos, cero valores hardcodeados. No tocar contraseñas ni el flujo de import.
  </action>
  <verify>
    <automated>node --check api/server.js && grep -c "auto_linked" api/server.js | awk '$1>=3{print "ok"}' && grep -n "role !== 'admin'\|role!=='admin'" api/server.js</automated>
  </verify>
  <done>
`node --check` pasa. `listGhlUsers` ejecuta en orden: auto-link por email exacto (solo activos sin ghl_user_id, PATCH solo ghl_user_id) → baja de huérfanos activos no-admin sin ghl_user_id (patrón PATCH+ban existente) → baja de desvinculados existente. Los auto-vinculados salen en `users` con status "importado". Respuesta incluye `auto_linked` (array de nombres). Los admin jamás entran en la baja de huérfanos.
  </done>
</task>

<task type="auto">
  <name>Task 2: UI — aviso de auto-vinculados y refresh inmediato del equipo</name>
  <files>index.html</files>
  <action>
En index.html:

1. **Estado:** en la línea ~1042, agregar `GHL_AUTO_LINKED=[]` junto a `GHL_REMOVED`. En el handler de desconexión (~1035) resetear también `GHL_AUTO_LINKED=[]` donde hoy se resetea `GHL_REMOVED=[]`.

2. **Aviso en ghlTeamCard:** junto al `removedBox` existente (~1056-1060), agregar un `autoLinkedBox` con el mismo patrón visual (`<div class="admin-lock">`), texto singular/plural en castellano: "Se vincularon automáticamente con su usuario de HighLevel: <b>{nombres}</b>" (singular: "Se vinculó automáticamente con su usuario de HighLevel: ..."). Usar `esc()` en los nombres como hace removedBox. Renderizarlo en el return del section junto a removedBox (orden: autoLinkedBox antes o después de removedBox, a discreción — mantener ambos visibles si ambos tienen contenido).

3. **loadGhlUsers (~1106-1132):** después de `GHL_REMOVED=data.removed||[];` agregar `GHL_AUTO_LINKED=data.auto_linked||[];`. Cambiar la condición de refresh: `if(GHL_REMOVED.length || GHL_AUTO_LINKED.length) await loadFromSupabase();` — así los dados de baja desaparecen de "Miembros del equipo" al instante y los auto-vinculados reflejan su nuevo estado. El `renderTeam()` final existente (línea ~1131) ya corre después del await, no duplicarlo.

No tocar importGhlUser ni el resto del flujo.
  </action>
  <verify>
    <automated>grep -c "GHL_AUTO_LINKED" index.html | awk '$1>=4{print "ok"}' && grep -n "GHL_REMOVED.length || GHL_AUTO_LINKED.length\|GHL_AUTO_LINKED.length || GHL_REMOVED.length" index.html && SUPABASE_URL=http://localhost SERVICE_ROLE_KEY=x ANON_KEY=x PORT=3987 timeout 5 node api/server.js 2>&1 | head -5</automated>
  </verify>
  <done>
`GHL_AUTO_LINKED` existe como estado, se resetea en disconnect, se puebla desde `data.auto_linked`, dispara `await loadFromSupabase()` cuando hay removed o auto_linked, y ghlTeamCard muestra el aviso de auto-vinculados con nombres escapados. La API arranca sin env GHL (log "modo manual") confirmando que nada de lo nuevo rompe el arranque.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente admin → GET /api/ghl/users | endpoint ya exige caller admin; la reconciliación corre con SERVICE_ROLE_KEY (bypass RLS) |
| GHL API → server | lista de usuarios externa dispara mutaciones (PATCH/ban) sobre perfiles propios |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q2.2-01 | Elevation | baja automática de huérfanos | mitigate | excepción `role === 'admin'` inamovible — el dueño nunca se auto-bloquea |
| T-q2.2-02 | Tampering | auto-link por email | mitigate | match EXACTO lowercase/trim server-side vía getAuthEmail (GoTrue admin), nunca datos del body del cliente; guard byGhlId evita robar un ghl_user_id ya tomado |
| T-q2.2-03 | Info Disclosure | respuesta JSON | mitigate | `auto_linked` solo lleva nombres (patrón idéntico a `removed`); jamás tokens ni emails de auth |
| T-q2.2-04 | DoS | fallo parcial de GHL/Supabase | accept | patrón existente: error por perfil → log + continue; una carga posterior reconcilia |
</threat_model>

<verification>
- `node --check api/server.js` pasa.
- API arranca sin env GHL con log "modo manual" (sin crash).
- `grep auto_linked api/server.js` muestra: acumulador, push y sendJSON.
- `grep GHL_AUTO_LINKED index.html` muestra: declaración, reset en disconnect, asignación desde data, uso en ghlTeamCard y en la condición de refresh.
- Revisión de código: el auto-link corre ANTES del map de `users` (status "importado" correcto); la baja de huérfanos excluye admin; paso 3 intacto.
</verification>

<success_criteria>
- Con GHL conectado, la primera carga de la lista deja a todo miembro activo no-admin o bien vinculado a un usuario GHL (email exacto) o bien dado de baja — cero miembros manuales fantasma.
- "Nico" (activo, sin ghl_user_id, email inexistente en GHL) cae automáticamente; un perfil activo cuyo email SÍ existe en GHL queda vinculado y sale "importado"; los admin sobreviven siempre.
- La UI avisa tanto bajas como auto-vinculaciones y "Miembros del equipo" se refresca al instante.
- Repo público sin secretos; contraseñas intactas.
</success_criteria>

## Post-merge (orquestador — NO ejecutor)

El ejecutor NO hace SSH ni deploy. Tras el merge a develop, el orquestador:

1. Deploy a `sales-tracker-test`.
2. QA e2e con la org de prueba (GHL conectado):
   - Cargar "Usuarios de HighLevel" → **Nico** (closer manual, nico@camino.digital, sin match en GHL) debe darse de baja automáticamente en esa primera carga y desaparecer de "Miembros del equipo" sin recargar la página; aviso de baja visible.
   - **admin@camino.digital** (role admin, sin ghl_user_id) debe sobrevivir intacto y seguir logueado.
   - **Ale Vogeler** (importado, con ghl_user_id) debe seguir intacto con status "importado".
   - Si existe un perfil activo con email que coincide con un usuario GHL → debe auto-vincularse, salir en el aviso de auto-vinculados y mostrarse "importado".

<output>
Al terminar, crear `.planning/quick/260704-tu4-fase-2-2-carcasa-ghl-reconciliaci-n-tota/260704-tu4-SUMMARY.md`
</output>
