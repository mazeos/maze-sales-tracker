---
phase: quick-260705-020
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [api/server.js, index.html]
autonomous: true
requirements: [QUICK-260705-020]

must_haves:
  truths:
    - "Al hacer foco en el input de subcuenta (aunque esté vacío), se abre un panel dropdown con las subcuentas de la agencia"
    - "El panel muestra hasta 50 resultados ordenados alfabéticamente, con header 'Mostrando X de Y subcuentas'"
    - "Cada fila muestra avatar con iniciales, nombre semibold y meta muted (ciudad · país · id corto); las vinculadas van al final, deshabilitadas, con badge 'vinculada a {org}'"
    - "↑/↓ mueven el highlight (con scroll), Enter selecciona, Escape cierra, click fuera cierra"
    - "El chip de selección muestra avatar + nombre + id + ✕, y crear org sigue mandando location_id"
  artifacts:
    - path: "api/server.js"
      provides: "listAgencyLocations con sort localeCompare 'es', cap 50 y respuesta {locations, total}"
      contains: "localeCompare"
    - path: "index.html"
      provides: "Combobox de subcuentas: panel absoluto, filas ricas, teclado, estados, click-outside"
      contains: "platLocOpen"
  key_links:
    - from: "index.html (platLocSearch)"
      to: "/api/platform/locations"
      via: "fetch con Bearer token, debounce 400ms"
      pattern: "api/platform/locations"
    - from: "index.html (pickPlatLoc)"
      to: "PLAT_LOC_RESULTS[i]"
      via: "selección por índice — nunca datos GHL en atributos onclick"
      pattern: "pickPlatLoc\\(\\$\\{?i"
    - from: "index.html (createOrg)"
      to: "POST /api/orgs body.location_id"
      via: "PLAT_LOC_SEL.id"
      pattern: "body\\.location_id=PLAT_LOC_SEL\\.id"
---

<objective>
Mejorar la UX del buscador de subcuentas en la vista Plataforma (form de alta de organizaciones): convertir el input + lista plana actual en un combobox cómodo — exploración al foco con input vacío, panel dropdown posicionado, filas ricas con avatar/meta, navegación por teclado, estados de carga/vacío/error, y header "Mostrando X de Y".

Purpose: hoy el super-admin tiene que adivinar el nombre exacto de la subcuenta; con exploración al foco + orden alfabético + teclado, elegir la subcuenta correcta toma segundos.
Output: `api/server.js` (sort + cap 50 + total) e `index.html` (combobox completo) modificados; sin dependencias nuevas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/alevogeler/maze-sales-tracker/CLAUDE.md
@/Users/alevogeler/maze-sales-tracker/index.html (líneas ~1340-1470: estado PLAT_LOC_*, orgsCard, platLocSearch, pickPlatLoc, clearPlatLoc; líneas ~55-190: tokens CSS .chip/.inp/.li/.empty; línea ~1794: esc(); ~1802: toast(); ~255+: overrides body[data-theme="light"])
@/Users/alevogeler/maze-sales-tracker/api/server.js (líneas ~1920-2009: searchAgencyLocations + listAgencyLocations — NO leer el resto del archivo)

<interfaces>
Contratos existentes que el ejecutor debe respetar (extraídos del código):

api/server.js — `searchAgencyLocations(pit, q)` devuelve `[{id, name, city, country}]` (email queda server-side, solo para filtrar). Cache en memoria 5 min (`agencyLocationsCache`). `listAgencyLocations(req, res, sa, url)` anota `linked_org` por fila y hoy responde `{locations}` con `.slice(0, 20)`.

index.html — estado global: `PLAT_LOC_SEL` ({id,name} o null), `PLAT_LOC_RESULTS` (array de la última búsqueda, selección por índice), `_locT` (timer debounce). `platLocSearch(q)` = debounce 400ms → fetch → pinta SOLO `#platLocResults` (nunca re-render completo: se pierde el foco del input). `pickPlatLoc(i)` guarda `PLAT_LOC_SEL` y llama `renderPlatform()`. `createOrg()` manda `body.location_id=PLAT_LOC_SEL.id`. Helpers: `esc(s)` (escape HTML obligatorio para TODO dato GHL), `toast(msg,ms)`. Tokens CSS: `--surface`, `--surface-2`, `--line`, `--ink`, `--muted`, `--faint`, `--lima`; tema claro redefine tokens vía `body[data-theme="light"]` → estilos con tokens funcionan en ambos temas.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: api/server.js — orden alfabético, cap 50 y total en listAgencyLocations</name>
  <files>api/server.js</files>
  <action>
Modificar SOLO `searchAgencyLocations` / `listAgencyLocations` (líneas ~1920-2009):

1. En `searchAgencyLocations`, antes del `return`, ordenar `filtered` por nombre: `localeCompare` con locale `'es'` y `{ sensitivity: 'base' }` (case-insensitive), comparando `String(a.name || a.id)` vs `String(b.name || b.id)`. Ordenar una copia o el array `filtered` (que ya es un array nuevo de `.filter()`) — NUNCA mutar `agencyLocationsCache.data` de forma que afecte al cache compartido (ordenar `filtered` es seguro porque `.filter()` crea array nuevo).
2. En `listAgencyLocations`: guardar `const total = locations.length` (cantidad tras el filtro, ANTES del cap), cambiar `.slice(0, 20)` por `.slice(0, 50)`, y responder `sendJSON(res, 200, { locations: out, total })`.
3. Actualizar los comentarios que quedan desactualizados: el bloque sobre "Máximo 20 resultados" (~línea 1971) pasa a decir cap de 50 + campo `total`; agregar mención del orden alfabético es-AR en el comentario de `searchAgencyLocations`.

No tocar el cache de 5 minutos, la paginación anti-DoS, el filtro por name/email/id, ni la anotación `linked_org`.
  </action>
  <verify>
    <automated>node --check /Users/alevogeler/maze-sales-tracker/api/server.js && grep -c "localeCompare" /Users/alevogeler/maze-sales-tracker/api/server.js && grep -n "slice(0, 50)" /Users/alevogeler/maze-sales-tracker/api/server.js && grep -n "locations: out, total" /Users/alevogeler/maze-sales-tracker/api/server.js</automated>
  </verify>
  <done>server.js pasa `node --check`; la respuesta es `{locations, total}` con hasta 50 filas ordenadas por nombre (localeCompare 'es', case-insensitive); total = cantidad post-filtro pre-cap; comentarios actualizados.</done>
</task>

<task type="auto">
  <name>Task 2: index.html — combobox de subcuentas (foco, panel, filas ricas, teclado, estados, chip)</name>
  <files>index.html</files>
  <action>
Reescribir el buscador de subcuentas del form de alta (estado ~línea 1349, `locField` en `orgsCard` ~1407-1414, `platLocSearch`/`pickPlatLoc`/`clearPlatLoc` ~1428-1467) como combobox. Copy en castellano latino. Reglas inviolables que se MANTIENEN: debounce 400ms en `oninput`, `esc()` sobre TODO dato que venga de GHL (name, city, country, id, linked_org) antes de cualquier `innerHTML`, selección por índice sobre `PLAT_LOC_RESULTS` (jamás inyectar name/id de GHL en atributos onclick), envío de `body.location_id=PLAT_LOC_SEL.id` en `createOrg()` intacto, y pintar SOLO el panel (nunca `renderPlatform()` completo mientras se tipea — se pierde el foco).

**(1) CSS** — agregar al stylesheet clases nuevas usando SOLO tokens existentes (funcionan en ambos temas; NO hardcodear colores salvo sombras rgba neutras):
- `.cbx-wrap{position:relative}`
- `.cbx-panel{position:absolute;top:calc(100% + 6px);left:0;right:0;z-index:60;max-height:320px;overflow-y:auto;background:var(--surface-2);border:1px solid var(--line);border-radius:12px;box-shadow:0 12px 32px rgba(0,0,0,.35);padding:6px}`
- `.cbx-head{font-size:11.5px;color:var(--muted);padding:6px 10px 8px;border-bottom:1px solid var(--line);margin-bottom:4px}`
- `.cbx-item{display:flex;align-items:center;gap:10px;padding:8px 10px;border-radius:9px;cursor:pointer;border:1px solid transparent}`
- `.cbx-item.hl{background:var(--surface);border-color:var(--lima)}`
- `.cbx-item.off{opacity:.45;cursor:default}`
- `.cbx-ava{width:28px;height:28px;border-radius:50%;background:var(--surface);border:1px solid var(--line);display:grid;place-items:center;font-size:11px;font-weight:700;color:var(--ink);flex:none}`
- `.cbx-name{font-size:13.5px;font-weight:600;color:var(--ink)}`
- `.cbx-meta{font-size:11.5px;color:var(--muted)}`
- `.cbx-badge{margin-left:auto;font-size:11px;color:var(--muted);border:1px solid var(--line);border-radius:100px;padding:2px 8px;white-space:nowrap}`

**(2) Estado** — ampliar la línea 1349: agregar `PLAT_LOC_TOTAL=0`, `PLAT_LOC_HL=-1` (índice highlighteado), `PLAT_LOC_OPEN=false`. Helper `locInitials(name)`: primeras letras de las dos primeras palabras, uppercase (fallback `'?'`). Helper `locShortId(id)`: primeros 8 caracteres.

**(3) Markup del campo** (rama sin selección de `locField`): envolver input + panel en `<div class="cbx-wrap" id="platLocWrap">`. Input: mantener `id="platLocQ"`, `autocomplete="off"`, `oninput="platLocSearch(this.value)"`, y agregar `onfocus="platLocFocus()"`, `onkeydown="platLocKey(event)"`, atributos `role="combobox" aria-expanded="false" aria-autocomplete="list"`. Panel: `<div class="cbx-panel" id="platLocResults" role="listbox" style="display:none"></div>`.

**(4) Fetch y render del panel** — refactorizar `platLocSearch` en dos partes:
- `platLocFetch(q)` (async, sin debounce): pinta `"Buscando…"` en el panel (visible), hace el fetch actual a `/api/platform/locations?q=`, y al volver: guarda `PLAT_LOC_TOTAL=data.total ?? (data.locations||[]).length`; reordena resultados con las vinculadas (`linked_org`) AL FINAL (estable: no-vinculadas primero en su orden, vinculadas después) y guarda ese array reordenado en `PLAT_LOC_RESULTS` (el índice de click/Enter apunta a este array); resetea `PLAT_LOC_HL` al índice de la primera fila NO vinculada (o -1 si no hay); llama `platLocPaint()`. Errores: sin sesión → toast actual; fetch fallido → panel con `.empty` "No se pudo conectar con el servidor"; `!res.ok` → panel con `esc(data.error || 'No se pudieron buscar las subcuentas')` (mantiene el hint del 409 de token no configurado).
- `window.platLocSearch=(q)=>{ clearTimeout(_locT); _locT=setTimeout(()=>platLocFetch(q),400); }` (debounce intacto).
- `platLocPaint()`: si `!PLAT_LOC_RESULTS.length` → `<div class="empty" style="padding:10px">No hay subcuentas que coincidan</div>`. Si hay: header `.cbx-head` con `Mostrando ${n} de ${PLAT_LOC_TOTAL} subcuentas` y, si `PLAT_LOC_TOTAL > n`, agregar ` — seguí escribiendo para afinar`. Filas: `.cbx-item` con avatar `.cbx-ava` (iniciales), columna con `.cbx-name` (nombre) y `.cbx-meta` (partes no vacías de [city, country, id corto] unidas por " · "); si `linked_org` → clase extra `off`, SIN onclick, badge `.cbx-badge` "vinculada a {org}"; si no → `onclick="pickPlatLoc(${i})"` y clase `hl` cuando `i===PLAT_LOC_HL`. TODO valor GHL pasa por `esc()`.

**(5) Abrir/cerrar** — `platLocFocus()`: marca `PLAT_LOC_OPEN=true`, muestra el panel, setea `aria-expanded="true"`, dispara `platLocFetch(valor actual del input)` de inmediato (sin debounce; q vacío = explorar todas — el cache server de 5 min lo hace barato), y registra en `document` un listener `pointerdown` (guardado en variable módulo, p.ej. `_locDocL`) que cierra el panel si el click NO cae dentro de `#platLocWrap`. `platLocClose()`: oculta el panel, `PLAT_LOC_OPEN=false`, `PLAT_LOC_HL=-1`, `aria-expanded="false"`, y REMUEVE el listener de document (cleanup obligatorio: nunca acumular listeners entre aperturas/re-renders).

**(6) Teclado** — `platLocKey(e)`: con el panel abierto — `ArrowDown`/`ArrowUp` (preventDefault) mueven `PLAT_LOC_HL` solo entre filas NO vinculadas (como están al final, es avanzar/retroceder dentro del prefijo seleccionable, con tope en los extremos, sin wrap), repintan y hacen `scrollIntoView({block:'nearest'})` sobre la fila highlighteada (darle `id` o buscarla por índice con `querySelector`); `Enter` (preventDefault) → si `PLAT_LOC_HL>=0` llama `pickPlatLoc(PLAT_LOC_HL)`; `Escape` → `platLocClose()`. Con panel cerrado, `ArrowDown` lo reabre (llama `platLocFocus()`).

**(7) Selección y chip** — `pickPlatLoc(i)`: mantiene el guard `if(!l || l.linked_org) return;` y ahora también llama `platLocClose()` ANTES de `renderPlatform()` (para limpiar el listener de document, ya que el re-render destruye el panel). El chip de la rama seleccionada de `locField` pasa a: `<button class="chip" type="button" onclick="clearPlatLoc()" data-tip="Quitar la subcuenta elegida"><span class="cbx-ava" style="width:22px;height:22px;font-size:10px">${esc(iniciales)}</span> ${esc(PLAT_LOC_SEL.name)} <span class="k">${esc(locShortId(PLAT_LOC_SEL.id))}</span> ✕</button>`. Para eso, `PLAT_LOC_SEL` sigue siendo `{id,name}` (alcanza para iniciales e id corto). `clearPlatLoc()` queda igual.

NO tocar: `createOrg()`, `loadOrgs()`, la tabla de orgs, el box de credenciales, ni nada fuera del buscador.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const html=fs.readFileSync('/Users/alevogeler/maze-sales-tracker/index.html','utf8');const js=[...html.matchAll(/<script(?![^>]*src)[^>]*>([\s\S]*?)<\/script>/g)].map(m=>m[1]).join('\n;\n');fs.writeFileSync('/private/tmp/claude-501/-Users-alevogeler/1ed76094-d531-479d-9cf5-6a6c0d78c7ad/scratchpad/_inline.js',js);console.log('extraído',js.length,'chars')" && node --check /private/tmp/claude-501/-Users-alevogeler/1ed76094-d531-479d-9cf5-6a6c0d78c7ad/scratchpad/_inline.js && grep -c "platLocFocus\|platLocClose\|platLocKey\|cbx-panel" /Users/alevogeler/maze-sales-tracker/index.html && grep -n "body.location_id=PLAT_LOC_SEL.id" /Users/alevogeler/maze-sales-tracker/index.html</automated>
    <human-check>En la vista Plataforma: foco en el input abre el panel con todas las subcuentas ordenadas; header "Mostrando X de Y"; ↑/↓/Enter/Escape funcionan; vinculadas al final deshabilitadas; click fuera cierra; chip con avatar+nombre+id+✕; probar en tema oscuro y claro.</human-check>
  </verify>
  <done>JS inline extraído pasa `node --check`; el combobox abre al foco con q vacío, panel absoluto bajo el input (max-height 320px, scroll), filas ricas escapadas con esc(), teclado completo, estados Buscando/vacío/error, header X de Y con hint, chip mejorado, click-outside con cleanup del listener; `createOrg` sigue mandando `location_id`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GHL API → panel del super-admin | name/city/country/id/linked_org vienen de HighLevel y se inyectan en innerHTML |
| Browser → /api/platform/locations | endpoint solo super-admin (auth Bearer ya existente, sin cambios) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q020-01 | Tampering (XSS) | platLocPaint / chip en index.html | mitigate | esc() sobre TODO dato GHL antes de innerHTML; selección por índice, jamás datos GHL en atributos onclick (patrón ya establecido, se preserva) |
| T-q020-02 | Information Disclosure | listAgencyLocations | accept | Sigue devolviendo solo {id,name,city,country,linked_org}; el email queda server-side; subir el cap a 50 no expone campos nuevos y el endpoint ya es solo super-admin |
| T-q020-03 | DoS | searchAgencyLocations | accept | Cache 5 min + tope de 10 páginas ya existentes; el fetch al foco pega contra el cache |
</threat_model>

<verification>
- `node --check api/server.js` pasa.
- JS inline de index.html extraído pasa `node --check`.
- `grep "slice(0, 50)"` y `grep "locations: out, total"` en api/server.js.
- `grep "localeCompare"` en api/server.js.
- En index.html existen `platLocFocus`, `platLocClose`, `platLocKey`, `cbx-panel`, y sigue existiendo `body.location_id=PLAT_LOC_SEL.id` y `pickPlatLoc(${i})` (índice, no datos).
- Cero secretos nuevos en el repo (repo público). Sin dependencias nuevas. Sin SSH ni deploy por parte del ejecutor.
</verification>

<success_criteria>
- El buscador de subcuentas es un combobox: explora al foco (q vacío), hasta 50 resultados alfabéticos, header "Mostrando X de Y" + hint, filas ricas (avatar/nombre/meta), vinculadas al final deshabilitadas con badge, teclado ↑/↓/Enter/Escape, estados Buscando/vacío/error, chip mejorado, click fuera cierra con cleanup.
- Backend responde `{locations, total}` ordenado y con cap 50.
- Se preservan intactos: debounce 400ms, esc() universal, selección por índice, envío de location_id, cache de 5 min.
</success_criteria>

<output>
Create `.planning/quick/260705-020-combobox-c-modo-para-el-buscador-de-subc/260705-020-SUMMARY.md` when done.
</output>
